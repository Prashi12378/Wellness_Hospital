import { Prisma } from "@prisma/client";
import "dotenv/config";

// This file is used to configure Prisma with environment variables
// It is specifically useful when using a monorepo structure

export default {
    // Config options
};
